# 更新日志（Changelog）

本文件记录「企业差旅管理系统」各版本的功能变更说明。所有重要的功能新增、变更与修复都会记录在此，每个版本对应一个章节。

## 版本维护规范

每次发布新版本时，请按以下规则在本文档**顶部**追加新版本章节：

1. 版本号遵循语义化版本规则 `主版本.次版本.修订号`：
   - **主版本**：不兼容的重大变更（如架构重构、接口协议变更）
   - **次版本**：向下兼容的新功能
   - **修订号**：向下兼容的问题修复
2. 章节结构固定为 `## [版本号] - 日期`，内容按 `### 新增`、`### 变更`、`### 修复`、`### 移除` 分类列出，只描述**该版本相对于上一版本**的变化。
3. 每个功能点用一句话描述，语言精炼，面向使用者（业务人员与开发人员均可读懂）。
4. 日期格式 `YYYY-MM-DD`；发布日期未知时可暂记为 `未发布`，发布后补填。
5. 若某版本未发布功能变更（仅内部改动），可标注 `### 内部` 说明，便于追溯。

---

## [1.0.0] - 2026-08-11

企业差旅管理系统初始版本，全量功能如下：

### 系统概述

- 前后端分离的 Web 应用：React + Vite 前端、Node.js + Express 后端、本地 JSON 文件持久化存储。
- 支持两种角色：**管理员**（审批与用户管理）与**普通员工**（差旅申请）。
- 支持开发模式（前后端分离运行）与生产模式（前端构建产物由后端单端口托管）两种部署方式。

### 新增

#### 认证与账户
- 登录：用户名 + 密码认证，密码使用 bcrypt 加密存储，登录成功后签发 JWT 令牌。
- 登出：令牌加入黑名单失效，登出后的令牌不可再访问接口。
- 系统首次启动自动创建初始管理员账号（通过环境变量配置）。
- 账号状态控制：被禁用的账号无法登录。

#### 员工端（普通员工）
- 新建差旅申请：填写出差目的地、出发/返回日期、出差事由、交通工具、预计费用。
- 我的申请列表：仅展示本人提交的申请，支持按状态筛选、分页查看，按提交时间倒序排列。
- 申请详情：查看本人申请的全部信息。
- 撤回申请：待审核状态的申请可撤回，撤回后状态变为「已撤回」。
- 重新提交：被拒绝的申请可修改信息后重新提交，生成新申请并关联原申请。

#### 管理员端
- 用户管理：
  - 查看全部员工账号列表。
  - 创建员工账号（用户名、姓名、初始密码）。
  - 编辑员工信息（用户名、姓名、状态）。
  - 启用/禁用员工账号。
  - 重置员工密码。
- 差旅审批：
  - 查看全部差旅申请列表（含申请人姓名），支持按状态筛选、分页查看。
  - 查看任意申请详情。
  - 通过申请（可填写审核意见）。
  - 拒绝申请（必须填写审核意见）。

#### 业务规则与数据校验
- 申请字段校验：目的地 1-100 字、事由 1-500 字、交通工具限枚举值（火车/飞机/汽车/高铁/轮船/其他）、预计费用为非负数且最多两位小数、返回日期不得早于出发日期。
- 状态机约束：仅「待审核」可撤回或审批，「已拒绝」才可重新提交，非法状态操作返回明确错误提示。
- 权限控制：接口按角色鉴权，员工只能访问自己的申请，管理员接口仅限管理员访问。

#### 审计与安全
- 审计日志：记录用户创建/编辑/禁用/重置密码、申请通过/拒绝等关键操作（操作人、角色、目标、详情），持久化至 JSON 文件。
- 敏感信息保护：接口返回用户信息时剔除密码哈希字段。

#### 前端页面
- 登录页 `/login`
- 员工端：我的申请 `/employee/requests`、新建申请 `/employee/requests/new`、申请详情 `/employee/requests/:id`、重新提交 `/employee/requests/:id/resubmit`
- 管理端：用户管理 `/admin/users`、申请列表 `/admin/requests`、申请详情 `/admin/requests/:id`
- 路由守卫：未登录跳转登录页，越权访问被拦截。

#### 测试
- 基于 Playwright 的端到端（E2E）测试，覆盖核心业务流程。